// #include "measurement/Benchmark.h"

// Benchmark::Benchmark(Vector& V, double a, double b, double c, int seed, int costThreshold) : a(a), b(b), c(c), seed(seed), costThreshold(costThreshold) {}

// void Benchmark::run() {
//     this->timer.start();
// }